[Unit]
Description=Pirl Content Master Node
Requires=pirlnode.service

[Service]
EnvironmentFile=/etc/pirlnode-env

Type=simple
User={{ RUNAS_USER }}
Group={{ RUNAS_USER }}

ExecStart=/usr/local/bin/pirl-content-linux-amd64 daemon
Restart=always

[Install]
WantedBy=default.target
